use crate::utils::line_reader::read_lines;

pub fn a() {
    let mut res = 0;
    if let Ok(lines) = read_lines("./input_template") {
        for line in lines {
            if let Ok(ip) = line {
            }
        }
    }
    println!("A: {:?}", res);
}

pub fn b() {
    let mut res = 0;
    if let Ok(lines) = read_lines("./input_template") {
        for line in lines {
            if let Ok(ip) = line {
            }
        }
    }
    println!("B: {:?}", res);
}

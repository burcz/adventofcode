use crate::utils::line_reader::read_lines;

pub fn a() {
    let res = 0;
    if let Ok(lines) = read_lines("./input12") {
        for line in lines {
            if let Ok(_ip) = line {
            }
        }
    }
    println!("A: {:?}", res);
}

pub fn b() {
    let res = 0;
    if let Ok(lines) = read_lines("./input12") {
        for line in lines {
            if let Ok(_ip) = line {
            }
        }
    }
    println!("B: {:?}", res);
}

